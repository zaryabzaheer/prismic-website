import React from 'react'
import { PrismicRichText } from '@prismicio/react'

/**
 * @typedef {import("@prismicio/client").Content.MemberSlice} MemberSlice
 * @typedef {import("@prismicio/react").SliceComponentProps<MemberSlice>} MemberProps
 * @param { MemberProps }
 */
const Member = ({ slice }) => {
  return (
    <section>
      <style jsx>{`
      `}</style>
    </section>
  )
}

export default Member
