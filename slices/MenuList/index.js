import React from 'react'
import { PrismicRichText } from '@prismicio/react'

/**
 * @typedef {import("@prismicio/client").Content.MenuListSlice} MenuListSlice
 * @typedef {import("@prismicio/react").SliceComponentProps<MenuListSlice>} MenuListProps
 * @param { MenuListProps }
 */
const MenuList = ({ slice }) => {
  return (
    <section>
      <style jsx>{`
      `}</style>
    </section>
  )
}

export default MenuList
